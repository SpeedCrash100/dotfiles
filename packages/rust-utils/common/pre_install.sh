#!/usr/bin/env bash

cargo install gitui --locked
cargo install bottom --locked
cargo install lsd --locked
cargo install dust --locked
cargo install hyperfine --locked

# TODO: Add alias for bat to cat
cargo install bat --locked